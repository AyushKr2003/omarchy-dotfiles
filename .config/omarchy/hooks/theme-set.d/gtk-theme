#!/usr/bin/env bash
set -euo pipefail

for dir in \
    "$HOME/.config/gtk-3.0" \
    "$HOME/.config/gtk-4.0"
do
    mkdir -p "$dir"

    cat > "$dir/gtk.css" <<'EOF'
/* Sharp corners, Hyprland-inspired */
* {
    border-radius: 0;
}
EOF
done
